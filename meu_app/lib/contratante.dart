import 'package:flutter/widgets.dart';

void main()=>runApp(Contratante());
class Contratante extends StatelessWidget {
  const Contratante({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}